process {
    withName: 'extractMafFromHal_ancestral|extractMafFromHal_target|extractTreeFromHal|extractGenomeFasta|liftOverRepeatsBed' {
        beforeScript = 'source /private/home/bcolquit/.bashrc && mamba activate cactus'
    }
}

executor {
    queueSize = 100
}
